import static org.junit.Assert.*;

public class ${NAME}Test extends BaseTest<${NAME}> {

    private ${NAME} mActivity;
    
    public ${NAME}Test() {
        super(${NAME}.class);
    }
  
    ${BODY}
}